{
    "_name": "{{ item.title Search }}",
    "_alias": "{{ item.title }}",
    "_urlTemplate": "{{ item.linktemplate }}",
    {% if item.description -%}
    "_description": "{{ item.description }}",
    {%- else -%}
    "_description": "Search {{ item.title }}",
    {%- endif %}
    "_urlParams": [],
    "_hidden": false,
    {%- if item.icon %}
    "_icon": "{{ item.icon }}",
    {% endif -%}
     "_urlNamespaces": {
	"rft": "info:ofi/fmt:kev:mtx:journal",
	"z": "http://www.zotero.org/namespaces/openSearch#",
	"": "http://a9.com/-/spec/opensearch/1.1/"
    }
}